import React from 'react'

const ThreadView = (props) => {
    //const { thread } = props;

    return(
        <div>
            <h1> Thread View </h1>
        </div>
    )
}

export default ThreadView;